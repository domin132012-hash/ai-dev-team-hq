# Codex Manager Output

下面是一套适合当前目标的 AI 小队管理流程。

重点是：

先建立流程，不碰代码；之后每次只做小任务，先体检，再拆分，再验收。

---

# 1. 团队结构总结

你是整个 AI 开发团队的负责人。

Codex Manager 负责管理其他 AI 的工作。

团队分工如下：

| 角色 | 主要职责 | 是否直接改代码 |
|---|---|---|
| Codex Manager | 总负责人、拆任务、分配任务、验收、控风险 | 可以，但要最后执行或整合 |
| Product Agent | 整理需求、用户流程、功能边界 | 否 |
| Frontend Agent | 页面、组件、样式、交互 | 是 |
| Backend Agent | 接口、数据库、数据保存、权限 | 是 |
| AI Feature Agent | AI 提词、日语解释、逐句分析、输出结构 | 可能 |
| Test Agent | 测试、复现 bug、验收功能 | 否或少量测试代码 |
| Code Review Agent | 审查代码风险、重复、破坏性修改 | 否 |
| DevOps Agent | 启动、构建、环境变量、部署上线 | 是，但只改部署相关文件 |

核心原则：

每个 AI 都像一个员工，只负责自己那一块。

Codex Manager 负责防止大家乱改、重写、互相覆盖。

---

# 2. Codex Manager 工作方式

每个任务都按这个顺序走：

## 1. 先确认目标

需要确认：

1. 这个任务要解决什么？
2. 是 bug、功能、UI、AI 效果、部署，还是重构？
3. 用户最后能看到什么变化？

---

## 2. 先做项目体检

需要检查：

1. 项目结构
2. 启动方式
3. 已有功能
4. 关键文件
5. 是否已有相似实现

注意：

不要急着改代码。

---

## 3. 拆成小任务

要求：

1. 每次只做一个小目标
2. 一个任务只交给一个主要 Agent
3. 明确哪些文件可以碰
4. 明确哪些文件不能碰

---

## 4. 写任务卡

任务卡必须包含：

1. 任务背景
2. 要做什么
3. 不允许做什么
4. 可修改范围
5. 验收标准
6. 输出格式

---

## 5. 执行前检查

需要检查：

1. 会不会删除已有功能？
2. 会不会重写大块代码？
3. 会不会影响别人的文件？
4. 有没有验收标准？

---

## 6. 执行后验收

需要做：

1. 跑项目
2. 跑测试
3. 检查页面、接口、AI 输出
4. Code Review Agent 审查
5. Test Agent 验证

---

## 7. 总结记录

需要记录：

1. 改了什么
2. 改了哪些文件
3. 验收结果
4. 还剩什么问题

---

# 3. 第一次项目体检流程

第一次体检只读项目，不改代码。

体检目标：

搞清楚“这个项目是什么、怎么跑、哪里能改、哪里不能乱动”。

---

## 1. 查看项目结构

检查：

1. 根目录有哪些文件
2. 是前端项目、后端项目，还是全栈项目
3. 是否有 `package.json`
4. 是否有 `README`
5. 是否有数据库配置
6. 是否有部署配置

---

## 2. 查看技术栈

检查：

1. 前端框架：React / Next.js / Vue / 其他
2. 后端框架：Node / Python / Ruby / Java / 其他
3. 数据库：SQLite / PostgreSQL / MySQL / Supabase / Firebase / 其他
4. AI 接口：OpenAI / Claude / Gemini / 本地模型 / 其他

---

## 3. 查看启动方式

检查：

1. 本地怎么安装依赖
2. 本地怎么启动
3. 是否有 `.env.example`
4. 需要哪些环境变量

---

## 4. 查看已有功能

检查：

1. 当前页面有哪些
2. 用户能做什么
3. 哪些功能已经能用
4. 哪些地方明显坏了

---

## 5. 查看关键模块

检查：

1. 页面入口
2. API 入口
3. 数据保存逻辑
4. AI 调用逻辑
5. 测试文件
6. 部署配置

---

## 6. 查看风险点

检查：

1. 有没有大文件
2. 有没有重复代码
3. 有没有混乱命名
4. 有没有直接写死的密钥
5. 有没有没有测试的核心逻辑

---

## 7. 输出体检报告

体检报告模板：

```text
【项目体检报告】

1. 项目类型：
2. 技术栈：
3. 启动方式：
4. 主要目录：
5. 已有功能：
6. 数据保存方式：
7. AI 功能位置：
8. 测试方式：
9. 部署方式：
10. 当前风险：
11. 建议第一个小任务：
```

---

# 4. 7 个员工的标准任务卡

下面这些任务卡可以直接复制给其他 AI 使用。

---

## Product Agent 任务卡

```text
你是 Product Agent，负责整理产品需求，不修改代码。

任务目标：
【在这里写本次要整理的功能或问题】

请你完成：
1. 用小白能懂的话说明这个功能要解决什么问题
2. 写出用户使用流程
3. 明确本次任务范围
4. 明确不做什么
5. 写出验收标准
6. 如果需求不清楚，列出需要确认的问题

严格限制：
- 不修改代码
- 不设计大而全的新系统
- 不删除已有功能
- 只围绕本次小任务

输出格式：
【需求说明】
【用户流程】
【本次范围】
【不做内容】
【验收标准】
【待确认问题】
```

---

## Frontend Agent 任务卡

```text
你是 Frontend Agent，负责前端页面、组件、样式和交互。

任务目标：
【在这里写具体前端任务】

修改前必须先做：
1. 阅读相关页面和组件
2. 找出现有 UI 风格
3. 确认可修改文件
4. 说明不会影响哪些已有功能

允许修改：
【列出允许修改的文件或目录】

禁止修改：
【列出禁止修改的文件或目录】

严格限制：
- 不允许重写整个页面
- 不允许删除已有功能
- 不允许改后端接口
- 不允许修改未授权文件
- 不允许和其他 Agent 同时改同一个文件

验收标准：
1. 页面能正常打开
2. 原有功能不丢失
3. 新增或修改的 UI 符合任务目标
4. 移动端和桌面端不明显错位
5. 没有控制台明显报错

输出格式：
【我阅读了哪些文件】
【我修改了哪些文件】
【具体修改内容】
【如何验证】
【风险说明】
```

---

## Backend Agent 任务卡

```text
你是 Backend Agent，负责后端、接口、数据保存和业务逻辑。

任务目标：
【在这里写具体后端任务】

修改前必须先做：
1. 阅读相关 API、数据模型、保存逻辑
2. 找出现有接口风格
3. 确认可修改文件
4. 确认不会破坏已有数据

允许修改：
【列出允许修改的文件或目录】

禁止修改：
【列出禁止修改的文件或目录】

严格限制：
- 不允许重写整个后端
- 不允许删除已有接口
- 不允许随意改数据库结构
- 不允许破坏已有数据
- 不允许修改前端文件，除非任务明确允许

验收标准：
1. 原有接口仍可用
2. 新接口或新逻辑符合任务目标
3. 数据能正确保存和读取
4. 错误情况有合理处理
5. 不泄露密钥或敏感信息

输出格式：
【我阅读了哪些文件】
【我修改了哪些文件】
【接口变化】
【数据变化】
【如何验证】
【风险说明】
```

---

## AI Feature Agent 任务卡

```text
你是 AI Feature Agent，负责 AI 提词、日语解释、逐句分析和输出格式。

任务目标：
【在这里写具体 AI 功能任务】

修改前必须先做：
1. 阅读现有 AI 调用逻辑
2. 阅读现有 prompt 或输出格式
3. 找出目前 AI 返回内容在哪里被使用
4. 确认修改不会破坏前端显示或后端保存

允许修改：
【列出允许修改的文件或目录】

禁止修改：
【列出禁止修改的文件或目录】

严格限制：
- 不允许改无关业务逻辑
- 不允许重写整个 AI 模块
- 不允许改变输出结构，除非任务明确要求
- 不允许删除已有 AI 功能

验收标准：
1. AI 输出更符合本次目标
2. 前端能正常显示 AI 结果
3. 保存逻辑不被破坏
4. 异常返回时有兜底处理
5. 日语解释准确、清楚、适合中文学习者

输出格式：
【我阅读了哪些文件】
【我修改了哪些 prompt/逻辑】
【输出格式是否变化】
【示例输出】
【如何验证】
【风险说明】
```

---

## Test Agent 任务卡

```text
你是 Test Agent，负责测试、复现 bug、找风险。

任务目标：
【在这里写要测试的功能】

请你完成：
1. 阅读本次任务说明
2. 找出需要测试的用户路径
3. 写出测试用例
4. 尝试找边界情况
5. 输出测试结果

严格限制：
- 不修改业务代码
- 不随意改配置
- 不删除文件
- 如果需要新增测试文件，必须先说明原因和文件位置

验收标准：
1. 覆盖正常流程
2. 覆盖失败流程
3. 覆盖至少 2 个边界情况
4. 明确说明通过或失败
5. 失败时给出复现步骤

输出格式：
【测试范围】
【测试用例】
【通过项】
【失败项】
【复现步骤】
【建议修复方向】
```

---

## Code Review Agent 任务卡

```text
你是 Code Review Agent，负责代码审查，不直接修改代码。

任务目标：
审查本次修改是否安全、清晰、符合项目原有风格。

请你检查：
1. 是否删除了已有功能
2. 是否有大规模重写
3. 是否修改了无关文件
4. 是否有明显 bug
5. 是否有安全风险
6. 是否有性能问题
7. 是否缺少测试或验收

严格限制：
- 不修改代码
- 不提出推倒重来的方案
- 不要求大重构，除非存在严重风险
- 只针对本次改动审查

输出格式：
【总体结论：通过/不通过】
【必须修复的问题】
【建议优化的问题】
【风险说明】
【是否符合“不推倒重来”原则】
```

---

## DevOps Agent 任务卡

```text
你是 DevOps Agent，负责启动、构建、环境变量和部署。

任务目标：
【在这里写具体部署或环境任务】

修改前必须先做：
1. 阅读 README、package.json、部署配置
2. 确认本地启动命令
3. 确认构建命令
4. 确认环境变量需求
5. 确认可修改文件

允许修改：
【列出允许修改的文件或目录】

禁止修改：
【列出禁止修改的文件或目录】

严格限制：
- 不允许修改业务功能
- 不允许删除已有部署配置
- 不允许泄露密钥
- 不允许把本地私密配置提交进项目
- 不允许大规模重写构建流程

验收标准：
1. 项目能本地启动
2. 项目能成功构建
3. 环境变量说明清楚
4. 部署步骤可复现
5. 不影响已有功能

输出格式：
【我阅读了哪些文件】
【启动方式】
【构建方式】
【环境变量】
【修改文件】
【验证结果】
【风险说明】
```

---

# 5. 哪些任务可以并行，哪些必须串行

## 可以并行的任务

| 可并行任务 | 原因 |
|---|---|
| Product Agent 整理需求 + Codex Manager 做项目体检 | 一个看产品，一个看代码结构，不冲突 |
| Test Agent 写测试用例 + Product Agent 写验收标准 | 都不改代码 |
| Code Review Agent 审查改动 + Test Agent 测试功能 | 一个看代码，一个跑功能 |
| DevOps Agent 检查部署配置 + Frontend Agent 看 UI 文件 | 前提是文件不重叠 |
| Backend Agent 看接口 + AI Feature Agent 看 prompt | 前提是 AI 逻辑和接口文件不重叠 |

---

## 必须串行的任务

| 串行任务 | 顺序 |
|---|---|
| 第一次项目体检和任何代码修改 | 必须先体检，再改代码 |
| Product Agent 需求整理和开发 | 先明确需求，再开发 |
| Backend 改接口和 Frontend 调接口 | 先确定接口，再接前端 |
| AI 输出结构变化和前端显示修改 | 先确定 AI 输出结构，再改前端显示 |
| 数据库结构变化和后端保存逻辑 | 先设计数据结构，再改保存逻辑 |
| 开发和 Code Review | 先开发，再审查 |
| 开发和最终测试 | 先开发，再测试 |
| 部署上线和功能验收 | 先验收，再部署 |

最重要的一条：

多个 AI 不能同时修改同一个文件。

如果两个任务都要改同一个文件，必须排队，一个完成并验收后，另一个再开始。

---

# 6. 禁止推倒重来的检查表

每次任务开始前，都要过这个检查表。

```text
【禁止推倒重来检查表】

任务名称：
负责人 Agent：

1. 本任务是否只解决一个小问题？
是 / 否

2. 是否已经完成项目体检？
是 / 否

3. 是否明确了允许修改的文件？
是 / 否

4. 是否明确了禁止修改的文件？
是 / 否

5. 是否会删除已有功能？
是 / 否

6. 是否会重写整个页面、模块或项目？
是 / 否

7. 是否会改动无关文件？
是 / 否

8. 是否会影响数据库或已有数据？
是 / 否 / 不涉及

9. 是否会改变接口返回格式？
是 / 否 / 不涉及

10. 是否会改变 AI 输出格式？
是 / 否 / 不涉及

11. 是否和其他 Agent 修改同一个文件？
是 / 否

12. 是否有清楚的验收标准？
是 / 否

13. 是否有回退或修复思路？
是 / 否

结论：
允许开始 / 不允许开始

如果“不允许开始”，原因是：
【写原因】
```

---

## 判断标准

只要出现下面任意情况，就不能开工：

1. 没做项目体检
2. 没有验收标准
3. 不知道要改哪些文件
4. 想一次性重写整个项目
5. 会删除已有功能
6. 多个 AI 要同时改同一个文件
7. 需求还没说清楚

---

# 7. 下一步应该怎么做

下一步不要马上开发功能，而是先让 Codex Manager 做第一次项目体检。

可以直接发这句话：

```text
请开始第一次项目体检。只读项目，不修改代码。请输出项目体检报告，并建议第一个最小开发任务。
```

Codex Manager 会做这些事：

1. 查看项目目录
2. 判断技术栈
3. 找启动命令
4. 找前端、后端、AI、数据、部署相关文件
5. 输出项目体检报告
6. 建议第一个小任务
7. 给出可以复制给对应 Agent 的任务卡

以后每次开发都按这个节奏走：

```text
体检 → 拆小任务 → 写任务卡 → 分配 Agent → 修改 → 测试 → Review → 验收 → 下一个小任务
```

这样 AI 团队就不会乱改项目，也不会一上来推倒重来。
